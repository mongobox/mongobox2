# FOS User Bundle
fos_user:
  db_driver: orm # other valid values are 'mongodb', 'couchdb' and 'propel'
  firewall_name: main
  user_class: App\Entity\User
  from_email:
    address:        noreply@mongobox.fr
    #sender_name:    "%site_name%"
    sender_name:    "MongoBox Troll Station!"
    #profile:
    #   form:
    #      type: aml_user_profile